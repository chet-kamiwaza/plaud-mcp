---
phase: 06-local-mac-validation-workflow
plan: 01
type: execute
wave: 1
depends_on: ["05-01", "05-02"]
files_modified: [scripts/verify-local-mac.sh, tests/test_local_mac_validation.py]
autonomous: true
requirements: [VAL-01]
user_setup:
  - Ensure Docker Desktop is installed and running before Docker validation
  - Ensure Podman is installed and `podman machine` is running before Podman validation
must_haves:
  truths:
    - The repo exposes a repeatable macOS-local verification entrypoint instead of requiring users to manually stitch together runtime and test commands.
    - Podman validation on macOS checks machine readiness, not only CLI installation.
    - The local validation flow reuses the Phase 5 runtime scripts and includes automated tests as part of the contract.
  artifacts:
    - scripts/verify-local-mac.sh
    - tests/test_local_mac_validation.py
  key_links:
    - scripts/verify-local-mac.sh calls scripts/verify-container-runtime.sh rather than duplicating compose logic.
    - scripts/verify-local-mac.sh runs pytest as part of the local verification flow so runtime checks and automated tests stay coupled.
    - tests/test_local_mac_validation.py asserts the script enforces Darwin-only behavior, Podman machine readiness checks, and runtime selection.
---

<objective>
Create a single repo-owned macOS validation command for local runtime checks so Podman setup and automated verification can be rerun deterministically on a Mac laptop.

Purpose: This satisfies the "repeatable local verification flow" requirement with an executable entrypoint instead of relying on loose manual command lists.
Output: A `verify-local-mac` script plus regression tests for its structure and safety checks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-local-mac-validation-workflow/06-CONTEXT.md
@.planning/phases/05-podman-runtime-compatibility/05-01-SUMMARY.md
@.planning/phases/05-podman-runtime-compatibility/05-02-SUMMARY.md
@scripts/container-runtime.sh
@scripts/verify-container-runtime.sh
@tests/test_runtime_scripts.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a macOS-local validation entrypoint for Podman and Docker</name>
  <files>scripts/verify-local-mac.sh</files>
  <read_first>scripts/verify-container-runtime.sh, scripts/container-runtime.sh, README.md</read_first>
  <action>Create `scripts/verify-local-mac.sh` as an executable shell script that accepts `podman`, `docker`, or `all`. Require macOS (`uname -s = Darwin`) and exit with a clear error on other platforms. For `podman`, verify both the CLI and a running `podman machine` before continuing. For `docker`, verify the Docker CLI and daemon readiness before continuing. For each selected runtime, call `bash scripts/verify-container-runtime.sh <runtime>`. In the same run, execute `pytest -q` so automated tests are part of the local validation contract. Keep the script non-interactive, fail fast, and avoid echoing Plaud secrets or `.env` contents.</action>
  <verify>`bash scripts/verify-local-mac.sh podman` succeeds on a macOS machine with Podman ready; `bash scripts/verify-local-mac.sh docker` succeeds on a macOS machine with Docker Desktop ready.</verify>
  <acceptance_criteria>
    - `scripts/verify-local-mac.sh` contains explicit `podman|docker|all` runtime handling
    - `scripts/verify-local-mac.sh` contains a Darwin check and a Podman machine readiness check
    - `scripts/verify-local-mac.sh` invokes `scripts/verify-container-runtime.sh` and `pytest -q`
  </acceptance_criteria>
  <done>The repo has one deterministic macOS validation command surface for Podman, Docker Desktop, or both.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for the macOS validation contract</name>
  <files>tests/test_local_mac_validation.py</files>
  <read_first>tests/test_runtime_scripts.py, scripts/verify-local-mac.sh, pyproject.toml</read_first>
  <action>Add `tests/test_local_mac_validation.py` with pytest checks that read `scripts/verify-local-mac.sh` as text and assert the expected behaviors are present: Darwin-only enforcement, Podman machine readiness checks, Docker readiness checks, runtime selection for `podman`, `docker`, and `all`, and invocation of `scripts/verify-container-runtime.sh` plus `pytest -q`. Update `pyproject.toml` only if strictly necessary for collection; otherwise leave it unchanged.</action>
  <verify>`pytest tests/test_local_mac_validation.py` exits 0.</verify>
  <acceptance_criteria>
    - `tests/test_local_mac_validation.py` asserts `uname -s` or equivalent Darwin enforcement
    - `tests/test_local_mac_validation.py` asserts Podman machine readiness markers and Docker readiness markers
    - `pytest tests/test_local_mac_validation.py` passes without requiring live Plaud credentials
  </acceptance_criteria>
  <done>The macOS validation script has structural regression coverage so future runtime changes cannot quietly break the local verification flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- macOS host shell -> local validation script
- local validation script -> Docker or Podman runtime readiness commands
- local validation script -> pytest runner and runtime verification script

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P6-01-T1 | Spoofing | runtime selection input | mitigate | Accept only `podman`, `docker`, or `all`; reject anything else with usage guidance. |
| P6-01-T2 | Denial of Service | Podman startup attempts on an unready machine | mitigate | Check Podman machine readiness explicitly before runtime verification so the script fails early with actionable guidance. |
| P6-01-T3 | Information Disclosure | local validation output | mitigate | Avoid printing `.env` contents or Plaud credentials; emit only runtime readiness and command-status messages. |
| P6-01-T4 | Repudiation | local validation contract drift | mitigate | Add pytest coverage that asserts the macOS validation script still performs Darwin gating, runtime readiness checks, and test invocation. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_local_mac_validation.py` passes
- [ ] `bash scripts/verify-local-mac.sh podman` succeeds on a Mac with Podman machine running
- [ ] `bash scripts/verify-local-mac.sh docker` succeeds on a Mac with Docker Desktop ready
- [ ] The script uses the existing Phase 5 runtime helper path instead of reimplementing compose commands directly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo defines one repeatable local verification flow for macOS that includes runtime checks and automated tests
- Podman machine readiness is enforced explicitly as part of the validation contract
</success_criteria>

<output>
After completion, create `.planning/phases/06-local-mac-validation-workflow/06-01-SUMMARY.md`
</output>
