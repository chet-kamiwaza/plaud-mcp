---
phase: 09-repo-surface-and-release-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, tests/test_release_assets.py]
autonomous: true
requirements: [REPO-01]
must_haves:
  truths:
    - Public package metadata describes the project accurately.
    - Release-facing metadata points readers at the repo, issue tracker, and README.
    - The repo gains automated coverage for the new release-facing metadata surface.
  artifacts:
    - pyproject.toml
    - tests/test_release_assets.py
  key_links:
    - pyproject.toml includes description, readme, urls, classifiers, and keywords that match the project.
    - tests/test_release_assets.py verifies the expected metadata and public docs references.
---

<objective>
Improve package metadata and add lightweight regression protection for the release-facing repo surface.

Purpose: Releasability is not just about prose; packaging metadata and public repo descriptors need to look intentional and stay correct.
Output: A stronger `pyproject.toml` plus tests guarding the release metadata contract.
</objective>

<context>
@.planning/phases/09-repo-surface-and-release-assets/09-CONTEXT.md
@pyproject.toml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Improve release-facing package metadata</name>
  <files>pyproject.toml</files>
  <action>Add PEP 621 metadata that describes the project for package consumers and repo visitors: description, readme, license file, keywords, classifiers, and repository URLs. Add the `build` tool to dev dependencies so the packaging path can be verified in Phase 10.</action>
  <verify>Manual inspection shows the metadata is accurate and `pyproject.toml` remains parseable.</verify>
  <acceptance_criteria>
    - `pyproject.toml` includes `description`, `readme`, `classifiers`, and `[project.urls]`
    - metadata matches the actual project behavior
    - dev dependencies include `build`
  </acceptance_criteria>
  <done>The package surface is credible for public release consumption.</done>
</task>

<task type="auto">
  <name>Add regression coverage for release assets</name>
  <files>tests/test_release_assets.py</files>
  <action>Create focused tests that validate the public metadata and docs linkage rather than internal implementation details. Keep the test structural and fast.</action>
  <verify>`pytest -q tests/test_release_assets.py` passes.</verify>
  <acceptance_criteria>
    - test file verifies expected package metadata fields
    - test file verifies the README references the public operations doc
  </acceptance_criteria>
  <done>Release-facing metadata regressions become visible in the normal test suite.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09-repo-surface-and-release-assets/09-01-SUMMARY.md`
</output>
