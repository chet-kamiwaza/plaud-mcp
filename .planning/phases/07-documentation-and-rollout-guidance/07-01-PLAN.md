---
phase: 07-documentation-and-rollout-guidance
plan: 01
type: execute
wave: 1
depends_on: ["05-01", "05-02", "06-01", "06-02"]
files_modified: [README.md]
autonomous: true
requirements: [DOC-01]
user_setup:
  - None beyond following the documented local setup steps on macOS
must_haves:
  truths:
    - The README explains how to install and prepare Podman on macOS for this repo, including machine startup expectations.
    - The README presents the validated repo-owned local commands for Podman and Docker Desktop instead of leaving users to derive the workflow from planning artifacts.
    - Quick-start and prerequisites language remain accurate for both supported runtimes and the existing Plaud auth modes.
  artifacts:
    - README.md
  key_links:
    - README.md references scripts/container-runtime.sh and scripts/verify-local-mac.sh with correct runtime-specific examples.
    - README.md describes Podman installation and machine readiness in the same document as the local startup steps.
    - README.md keeps Plaud auth mode instructions aligned with the existing .env example contract.
---

<objective>
Update the public README so a macOS user can install Podman, choose Docker Desktop or Podman intentionally, and use the validated repo-owned commands to start and verify the project locally.

Purpose: This satisfies the setup and prerequisite part of the documentation milestone and prevents the new runtime support from staying hidden in internal artifacts.
Output: A README quick-start and prerequisites section rewritten for dual-runtime local usage on macOS.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-documentation-and-rollout-guidance/07-CONTEXT.md
@.planning/phases/05-podman-runtime-compatibility/05-01-SUMMARY.md
@.planning/phases/05-podman-runtime-compatibility/05-02-SUMMARY.md
@.planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md
@README.md
@.env.example
@scripts/container-runtime.sh
@scripts/verify-local-mac.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite prerequisites and local setup for dual-runtime macOS usage</name>
  <files>README.md</files>
  <read_first>README.md, .env.example, scripts/container-runtime.sh, scripts/verify-local-mac.sh</read_first>
  <action>Update the README prerequisites and setup sections so Podman is documented alongside Docker Desktop for macOS. Add concrete Podman installation and `podman machine` setup guidance, keep Docker Desktop as a supported option, and explain the validated local verification command `bash scripts/verify-local-mac.sh <podman|docker>`. Make the wording explicit that the repo now supports either runtime on macOS and that the helper scripts are the canonical local workflow.</action>
  <verify>Open `README.md` and confirm it contains Podman installation/setup guidance, mentions `podman machine`, and includes `bash scripts/verify-local-mac.sh podman` plus the corresponding Docker path.</verify>
  <acceptance_criteria>
    - `README.md` contains Podman installation guidance for macOS
    - `README.md` contains `podman machine` setup/readiness guidance
    - `README.md` contains the repo-owned validation command for Podman and Docker Desktop
  </acceptance_criteria>
  <done>The README makes the validated macOS dual-runtime setup discoverable and actionable for users.</done>
</task>

<task type="auto">
  <name>Task 2: Update quick-start commands to use the repo-owned runtime helpers</name>
  <files>README.md</files>
  <read_first>README.md, scripts/container-runtime.sh, .planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md</read_first>
  <action>Revise the quick-start examples in the README so local container startup uses `bash scripts/container-runtime.sh docker up` or `bash scripts/container-runtime.sh podman up` instead of Docker-only `docker compose up -d` as the primary documented workflow. Preserve the existing Plaud auth-mode explanations, but make runtime selection explicit in the command examples and prerequisites. Keep non-container and image-build sections accurate without over-expanding scope.</action>
  <verify>Open `README.md` and confirm the quick-start examples reference `scripts/container-runtime.sh` for both Docker Desktop and Podman, while keeping the Plaud auth mode guidance intact.</verify>
  <acceptance_criteria>
    - `README.md` uses `scripts/container-runtime.sh` as the primary local container command surface
    - Runtime-specific examples are present for both Docker Desktop and Podman
    - Existing auth mode sections remain coherent after the runtime-doc rewrite
  </acceptance_criteria>
  <done>The README quick start reflects the actual validated runtime commands instead of the old Docker-only default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Public README -> local developer runtime actions
- Public README -> auth configuration behavior for Plaud credentials

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P7-01-T1 | Tampering | outdated runtime instructions | mitigate | Base all runtime examples on the repo-owned helper scripts and validated Phase 6 commands. |
| P7-01-T2 | Information Disclosure | documentation of auth configuration | mitigate | Keep examples aligned with `.env.example` and avoid introducing real secret values or unsafe copy-paste content. |
| P7-01-T3 | Denial of Service | Podman setup omissions on macOS | mitigate | Document `podman machine` readiness explicitly so users do not hit opaque startup failures. |
| P7-01-T4 | Repudiation | docs diverging from validated behavior | mitigate | Cross-check README edits against Phase 6 validation evidence before declaring the plan complete. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `README.md` contains Podman installation/setup guidance for macOS
- [ ] `README.md` contains `bash scripts/verify-local-mac.sh podman` and `bash scripts/verify-local-mac.sh docker`
- [ ] `README.md` uses `scripts/container-runtime.sh` as the primary local container command surface
- [ ] Auth mode instructions remain aligned with `.env.example`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The README documents how to install and validate Podman on macOS for this repo
- The README presents accurate dual-runtime setup steps based on validated repo commands
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation-and-rollout-guidance/07-01-SUMMARY.md`
</output>
