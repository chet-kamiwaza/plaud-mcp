---
phase: 06-local-mac-validation-workflow
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [.planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md]
autonomous: true
requirements: [VAL-02]
user_setup:
  - Keep host port 8080 free before running local runtime validation
must_haves:
  truths:
    - Phase 6 ends with concrete local validation evidence for Podman and Docker Desktop, not just a script that exists in the repo.
    - The validation record captures the exact commands, runtime prerequisites, and outcomes needed to prove Docker stayed working after Podman support was added.
    - Phase-local evidence stays inside `.planning` so public README work remains deferred to Phase 7.
  artifacts:
    - .planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md
  key_links:
    - 06-VALIDATION.md records runs of scripts/verify-local-mac.sh for Podman and Docker Desktop.
    - 06-VALIDATION.md includes pytest results so runtime validation and automated test coverage are documented together.
    - 06-VALIDATION.md captures runtime-specific caveats that Phase 7 documentation can later promote into user-facing guidance.
---

<objective>
Run the local macOS validation workflow against both supported runtimes and record durable evidence that Podman works locally while Docker Desktop remains non-broken.

Purpose: This converts the executable validation flow into milestone evidence that satisfies the dual-runtime regression requirement and feeds the documentation phase.
Output: A Phase 6 validation record with commands run, outcomes, and runtime-specific notes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-local-mac-validation-workflow/06-CONTEXT.md
@.planning/phases/06-local-mac-validation-workflow/06-01-PLAN.md
@scripts/verify-local-mac.sh
@scripts/verify-container-runtime.sh
@tests/test_local_mac_validation.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and record the Podman local validation path on macOS</name>
  <files>.planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md</files>
  <read_first>scripts/verify-local-mac.sh, scripts/verify-container-runtime.sh</read_first>
  <action>Execute the Phase 6 macOS validation entrypoint for Podman and capture the evidence in `.planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md`. Record the macOS date/time, `podman version`, Podman machine status, the exact command run, the exit status, and a concise summary of what passed. Include whether pytest passed in the same validation run and note any runtime caveats observed, such as external compose-provider behavior. Keep the artifact concise and secret-safe.</action>
  <verify>`bash scripts/verify-local-mac.sh podman` exits 0 and `.planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md` contains a Podman validation section with command + result.</verify>
  <acceptance_criteria>
    - `06-VALIDATION.md` contains a Podman runtime section
    - The Podman section records machine readiness and the command executed
    - The Podman section includes the automated test result from the same validation flow
  </acceptance_criteria>
  <done>Phase 6 contains durable evidence that the Podman local validation path works on the target Mac environment.</done>
</task>

<task type="auto">
  <name>Task 2: Run and record the Docker regression path on macOS</name>
  <files>.planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md</files>
  <read_first>.planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md, scripts/verify-local-mac.sh</read_first>
  <action>Extend `.planning/phases/06-local-mac-validation-workflow/06-VALIDATION.md` with the Docker Desktop validation run. Record the exact command, Docker readiness state, exit status, and concise result summary. Add a short comparison note describing any behavioral differences between Docker Desktop and Podman observed during validation, keeping the wording implementation-focused rather than end-user troubleshooting prose. Confirm that the Docker path passed after the Podman support changes and that port/cleanup expectations remained intact.</action>
  <verify>`bash scripts/verify-local-mac.sh docker` exits 0 and `06-VALIDATION.md` contains both Podman and Docker result sections plus a short runtime-differences note.</verify>
  <acceptance_criteria>
    - `06-VALIDATION.md` contains both Podman and Docker validation sections
    - The Docker section shows the repo-owned validation flow succeeded after Podman support was added
    - `06-VALIDATION.md` contains a brief runtime comparison note usable by Phase 7 docs work
  </acceptance_criteria>
  <done>Phase 6 contains dual-runtime local validation evidence proving Docker Desktop remains a working path alongside Podman.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- validation artifact authoring -> local runtime command output
- validation workflow -> Docker Desktop and Podman local runtimes
- phase evidence -> future documentation and milestone audit steps

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P6-02-T1 | Repudiation | validation evidence accuracy | mitigate | Record exact commands, timestamps, readiness state, and exit outcomes for each runtime instead of hand-waving success. |
| P6-02-T2 | Information Disclosure | validation artifact contents | mitigate | Summarize results without embedding `.env` values, tokens, or sensitive command output. |
| P6-02-T3 | Denial of Service | leftover containers/port bindings after regression runs | mitigate | Reuse the cleanup behavior in the validation script and explicitly note that no leftover `8080` listener remained after runs. |
| P6-02-T4 | Tampering | phase scope drift into public docs | mitigate | Keep the artifact inside `.planning` and limit notes to implementation evidence and runtime differences; leave README changes for Phase 7. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/verify-local-mac.sh podman` passes and is recorded in `06-VALIDATION.md`
- [ ] `bash scripts/verify-local-mac.sh docker` passes and is recorded in `06-VALIDATION.md`
- [ ] `06-VALIDATION.md` includes pytest results, runtime readiness state, and a concise runtime-differences note
- [ ] The plan does not modify the public README; user-facing documentation remains Phase 7 work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 ends with durable local validation evidence for both Podman and Docker Desktop on macOS
- Docker Desktop regression status is explicit rather than inferred
</success_criteria>

<output>
After completion, create `.planning/phases/06-local-mac-validation-workflow/06-02-SUMMARY.md`
</output>
