---
phase: 08-product-narrative-and-readme-repair
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [README.md]
autonomous: true
requirements: [DOCS-02]
must_haves:
  truths:
    - The README describes auth modes, transports, and runtime helpers in language that matches the code and validated scripts.
    - Podman and Docker guidance remains present but is organized as runtime support, not the product definition.
    - The README recommends the supported auth flows without hiding the legacy path.
  artifacts:
    - README.md
  key_links:
    - README.md documents environment and auth expectations that match src/plaud_mcp/config.py.
    - README.md references scripts/container-runtime.sh and scripts/verify-local-mac.sh as the canonical local container commands.
---

<objective>
Align the README details with the actual code paths for auth, transport, and runtime operation.

Purpose: A better narrative is not enough if the commands and mode descriptions are still misleading or internally inconsistent.
Output: A README whose setup and run instructions match the code and the validated local flows.
</objective>

<context>
@.planning/phases/08-product-narrative-and-readme-repair/08-CONTEXT.md
@README.md
@src/plaud_mcp/config.py
@src/plaud_mcp/__main__.py
@scripts/container-runtime.sh
@scripts/verify-local-mac.sh
</context>

<tasks>

<task type="auto">
  <name>Correct auth, transport, and runtime guidance</name>
  <files>README.md</files>
  <action>Document the three supported auth modes, clearly distinguish `stdio` from HTTP transport, and point local container usage at the repo-owned runtime and validation scripts. Keep the Docker and Podman instructions accurate for macOS and keep troubleshooting concise enough for a top-level README.</action>
  <verify>Manual doc review plus spot-checks against the referenced code and scripts show no contradictions.</verify>
  <acceptance_criteria>
    - README mentions `MCP_TRANSPORT` behavior consistently with `src/plaud_mcp/__main__.py`
    - README auth guidance matches the token-source rules in `src/plaud_mcp/config.py`
    - README names `scripts/container-runtime.sh` and `scripts/verify-local-mac.sh` as canonical local commands
  </acceptance_criteria>
  <done>The README setup guidance is accurate enough to be trusted for release.</done>
</task>

</tasks>

<verification>
- [ ] README auth and transport text matches the live code
- [ ] README local runtime commands match the repo-owned helper scripts
- [ ] Podman and Docker guidance remains accurate but no longer overwhelms the document
</verification>

<output>
After completion, create `.planning/phases/08-product-narrative-and-readme-repair/08-02-SUMMARY.md`
</output>
