---
phase: 05-podman-runtime-compatibility
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [scripts/verify-container-runtime.sh, tests/test_runtime_scripts.py, pyproject.toml]
autonomous: true
requirements: [RT-02]
user_setup:
  - Ensure either Docker Desktop or Podman is available before running runtime verification commands
must_haves:
  truths:
    - The repo contains an executable compatibility-check path that validates runtime prerequisites and service startup assumptions.
    - Runtime helper behavior is regression-tested so Podman support does not silently break Docker Desktop support.
    - The verification path checks the same env, volume, and localhost expectations that the container runtime uses in practice.
  artifacts:
    - scripts/verify-container-runtime.sh
    - tests/test_runtime_scripts.py
    - pyproject.toml
  key_links:
    - scripts/verify-container-runtime.sh calls the runtime helper from Plan 05-01 instead of duplicating compose logic.
    - tests/test_runtime_scripts.py asserts the runtime script behaviors and expected compose/runtime strings.
    - pyproject.toml exposes a test path that keeps the new runtime-script tests runnable in the existing pytest workflow.
---

<objective>
Add executable compatibility guardrails for the new local runtime flow so Phase 5 ends with enforceable Podman/Docker parity checks rather than only manual command changes.

Purpose: This makes the runtime contract testable and gives later validation/docs work a stable command surface to build on.
Output: A runtime verification script and automated tests that cover the helper behavior and compatibility assumptions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-podman-runtime-compatibility/05-CONTEXT.md
@.planning/phases/05-podman-runtime-compatibility/05-01-PLAN.md
@scripts/container-runtime.sh
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an executable runtime verification script</name>
  <files>scripts/verify-container-runtime.sh</files>
  <read_first>scripts/container-runtime.sh, docker-compose.yml, .env.example</read_first>
  <action>Create `scripts/verify-container-runtime.sh` as an executable shell script that accepts a runtime argument (`docker` or `podman`) and performs a deterministic compatibility check sequence. The script should: validate that the chosen runtime CLI exists; run the helper's `config` action; run the helper's `build` action; run the helper's `up` action; check that the rendered runtime or compose configuration still targets port `127.0.0.1:8080:8080`; and always run the helper's `down` action in a shell `trap` for cleanup. Keep the script non-interactive and exit non-zero on the first failed prerequisite or runtime action. Do not print Plaud secrets or dump `.env` contents.</action>
  <verify>`bash scripts/verify-container-runtime.sh docker` performs the config/build/up/down sequence on a Docker-enabled machine; `bash scripts/verify-container-runtime.sh podman` performs the same sequence on a Podman-enabled machine.</verify>
  <acceptance_criteria>
    - `scripts/verify-container-runtime.sh` contains `trap` and calls `scripts/container-runtime.sh`
    - `scripts/verify-container-runtime.sh` contains `build`, `up`, `down`, and `config`
    - `scripts/verify-container-runtime.sh` checks for `127.0.0.1:8080:8080` in rendered config or equivalent helper output
  </acceptance_criteria>
  <done>The repo has a reusable compatibility verification script that exercises the runtime helper end-to-end and cleans up reliably.</done>
</task>

<task type="auto">
  <name>Task 2: Add automated regression tests for runtime helper behavior</name>
  <files>tests/test_runtime_scripts.py, pyproject.toml</files>
  <read_first>tests/test_get_token.py, tests/test_client.py, scripts/container-runtime.sh, scripts/verify-container-runtime.sh, pyproject.toml</read_first>
  <action>Add `tests/test_runtime_scripts.py` with pytest tests that validate the new shell scripts as repo artifacts. The tests should read the script files as text and assert concrete behavior markers: Docker and Podman branches exist, the helper exposes the supported actions, the verify script invokes the helper and includes cleanup via `trap`, and the verify path checks the loopback port mapping. If needed, update `pyproject.toml` only minimally so the new test file is collected by the existing pytest configuration without changing the current test runner contract.</action>
  <verify>`pytest tests/test_runtime_scripts.py` exits 0.</verify>
  <acceptance_criteria>
    - `tests/test_runtime_scripts.py` contains assertions for `docker compose` and `podman compose`
    - `tests/test_runtime_scripts.py` contains assertions for `trap` and `127.0.0.1:8080:8080`
    - `pytest tests/test_runtime_scripts.py` passes without requiring live Plaud credentials
  </acceptance_criteria>
  <done>The runtime compatibility scripts are covered by automated tests that will fail if Docker or Podman support regresses structurally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Verification script -> runtime helper script
- Verification script -> Docker or Podman CLI
- Test runner -> shell script source files

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P5-02-T1 | Tampering | verification cleanup behavior | mitigate | Always register a `trap` that runs the helper's `down` action so failed checks do not leave the local service running. |
| P5-02-T2 | Denial of Service | repeated local runtime startup during verification | mitigate | Keep verification focused on one selected runtime at a time and ensure the cleanup path runs on success and failure. |
| P5-02-T3 | Information Disclosure | verification output | mitigate | Do not print `.env` contents or Plaud secrets; only print runtime/action progress and explicit failure messages. |
| P5-02-T4 | Repudiation | compatibility contract drift | mitigate | Add pytest coverage that asserts the runtime scripts contain the expected Docker/Podman branches, cleanup, and loopback checks. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_runtime_scripts.py` passes
- [ ] `scripts/verify-container-runtime.sh` calls `scripts/container-runtime.sh` and uses cleanup via `trap`
- [ ] The verification script checks loopback binding and runtime actions `config`, `build`, `up`, and `down`
- [ ] The plan does not modify README or broader docs; documentation work remains for Phase 7
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 ends with an executable compatibility-check path plus automated regression coverage for the runtime scripts
- Runtime invariant checks cover env contract, mounted state, and loopback-local exposure assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/05-podman-runtime-compatibility/05-02-SUMMARY.md`
</output>
